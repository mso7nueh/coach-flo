# Улучшенный промпт для AI Postman - с анализом результатов тестирования

Скопируйте и вставьте этот промпт в AI Postman для автоматического тестирования и исправления найденных проблем.

---

## 📋 Промпт для исправления проблем и тестирования

```
Проанализируй результаты предыдущего тестирования Postman коллекции "Coach Fit API" и исправь найденные проблемы:

## Анализ предыдущих результатов:

### Найденные проблемы:
1. **Register Client Step 1 и Register Trainer Step 1 возвращают 500 (Internal Server Error)**
   - Проверь логи сервера, чтобы понять причину ошибки
   - Убедись, что база данных запущена и таблицы созданы
   - Проверь, что все обязательные поля отправляются корректно

2. **Register Step 2 возвращает 400 (Bad Request)**
   - Это ожидаемо, так как Step 1 не прошел успешно
   - После исправления Step 1, Step 2 должен работать

3. **Множество запросов возвращают 401 (Unauthorized)**
   - Это ожидаемо, так как регистрация не прошла и токены не были сохранены
   - После успешной регистрации эти запросы должны работать

4. **Send SMS и Verify SMS работают (200 OK)**
   - Эти эндпоинты работают корректно

## План исправления и тестирования:

### Шаг 1: Диагностика проблемы регистрации

1. **Проверь логи сервера:**
   - Открой консоль, где запущен сервер
   - Выполни Register Client Step 1 и посмотри на ошибку в логах
   - Определи причину 500 ошибки

2. **Проверь подключение к базе данных:**
   - Убедись, что PostgreSQL запущен
   - Проверь переменные окружения для подключения к БД
   - Убедись, что все таблицы созданы (включая PendingRegistration)

3. **Проверь данные запроса:**
   - Убедись, что все обязательные поля присутствуют:
     * full_name (минимум 2 символа)
     * email (валидный email)
     * password (минимум 6 символов)
     * phone (валидный номер телефона)
     * role (client или trainer)

### Шаг 2: Исправление и повторное тестирование регистрации

1. **Исправь проблему с 500 ошибкой:**
   - Если проблема в БД - исправь подключение
   - Если проблема в данных - исправь запрос
   - Если проблема в коде - сообщи разработчику

2. **Протестируй регистрацию клиента:**
   - POST /api/auth/register/step1 (с уникальными данными: email и phone)
   - Проверь, что ответ 200 OK и SMS код отправлен
   - Проверь консоль сервера для получения SMS кода
   - POST /api/auth/register/step2 (с кодом из консоли)
   - Убедись, что токен сохранен в переменную client_token
   - Убедись, что user.id сохранен в переменную client_id

3. **Протестируй регистрацию тренера:**
   - POST /api/auth/register/step1 (с другими уникальными данными)
   - POST /api/auth/register/step2 (с кодом)
   - Убедись, что токен сохранен в переменную trainer_token
   - Убедись, что trainer_id сохранен

### Шаг 3: Тестирование полного флоу после исправления

После успешной регистрации протестируй следующие сценарии:

#### Флоу для клиента (после успешной регистрации):

1. **Онбординг:**
   - POST /api/onboarding/complete (с Authorization: Bearer {{client_token}})
   - GET /api/onboarding/ (проверь получение данных)

2. **Профиль:**
   - GET /api/users/me (проверь получение профиля)
   - PUT /api/users/me (обнови профиль)
   - GET /api/users/me/settings (получи настройки)
   - PUT /api/users/me/settings (обнови настройки)

3. **Тренировки:**
   - POST /api/workouts (создай тренировку)
   - GET /api/workouts (получи список)
   - Обнови и удали тренировку

4. **Программы, метрики, питание, дашборд** - аналогично

#### Флоу для тренера (после успешной регистрации):

1. **Управление клиентами:**
   - POST /api/clients (добавь клиента)
   - GET /api/clients (получи список)
   - Обнови и получи статистику клиента

2. **Тренировки для клиентов:**
   - POST /api/workouts (с user_id клиента)
   - GET /api/workouts?trainer_view=true

3. **Финансы, упражнения, заметки** - аналогично

### Шаг 4: Создание тестовых скриптов

Для каждого запроса создай тестовые скрипты, которые:

1. **Проверяют статус код:**
   ```javascript
   pm.test("Status code is 200", function () {
       pm.response.to.have.status(200);
   });
   ```

2. **Проверяют структуру ответа:**
   ```javascript
   pm.test("Response is valid JSON", function () {
       pm.response.to.be.json;
   });
   ```

3. **Сохраняют переменные:**
   ```javascript
   if (pm.response.code === 200) {
       var jsonData = pm.response.json();
       if (jsonData.token) {
           pm.collectionVariables.set("client_token", jsonData.token);
       }
       if (jsonData.user && jsonData.user.id) {
           pm.collectionVariables.set("client_id", jsonData.user.id);
       }
   }
   ```

4. **Проверяют обязательные поля:**
   ```javascript
   pm.test("Response has required fields", function () {
       var jsonData = pm.response.json();
       pm.expect(jsonData).to.have.property('id');
       pm.expect(jsonData).to.have.property('email');
   });
   ```

### Шаг 5: Обработка ошибок

Убедись, что тесты корректно обрабатывают:

- **400 Bad Request** - неверные данные
- **401 Unauthorized** - отсутствие токена или неверный токен
- **403 Forbidden** - недостаточно прав доступа
- **404 Not Found** - ресурс не найден
- **500 Internal Server Error** - внутренняя ошибка сервера

### Шаг 6: Создание отчета

Создай подробный отчет с:

1. **Списком исправленных проблем:**
   - Какая была проблема
   - Как она была исправлена
   - Результат после исправления

2. **Статистикой тестирования:**
   - Количество успешных запросов
   - Количество неудачных запросов
   - Процент покрытия API

3. **Рекомендациями:**
   - Что нужно исправить в API (если есть)
   - Что нужно улучшить в коллекции
   - Какие тесты добавить

## Важные замечания:

⚠️ **Для устранения 500 ошибки при регистрации:**

1. Проверь, что все обязательные поля отправляются:
   ```json
   {
     "full_name": "Иван Иванов",
     "email": "unique@example.com",  // ДОЛЖЕН БЫТЬ УНИКАЛЬНЫМ
     "password": "password123",      // Минимум 6 символов
     "phone": "+7 (999) 123-45-67",  // ДОЛЖЕН БЫТЬ УНИКАЛЬНЫМ
     "role": "client"
   }
   ```

2. Убедись, что база данных работает:
   - Проверь docker-compose up -d db (если используется Docker)
   - Проверь подключение к БД в env файле

3. Проверь логи сервера для понимания причины ошибки

⚠️ **Последовательность регистрации:**
- НЕ используй /send-sms перед регистрацией!
- Используй только /register/step1 → /register/step2
- SMS коды выводятся в консоль сервера

⚠️ **Переменные:**
- base_url = http://localhost:8000
- client_token и trainer_token должны сохраняться автоматически
- Используй уникальные email и phone для каждого теста

## Начни с диагностики:

1. Сначала найди и исправь причину 500 ошибки при регистрации
2. Затем протестируй регистрацию клиента и тренера
3. После успешной регистрации протестируй остальные эндпоинты
4. Создай тестовые скрипты для автоматической валидации
5. Сформируй отчет о результатах
```

---

## 🔧 Быстрый промпт для диагностики

Если нужно быстро найти проблему, используй этот сокращенный промпт:

```
Проанализируй результаты тестирования Postman коллекции "Coach Fit API":

1. Register Step 1 возвращает 500 ошибку - найди причину в логах сервера и исправь
2. После исправления протестируй полный флоу регистрации (step1 → step2) для клиента и тренера
3. Убедись что токены сохраняются в переменные после успешной регистрации
4. Протестируй остальные эндпоинты с правильными токенами в заголовке Authorization
5. Создай тестовые скрипты для автоматической проверки статус кодов и сохранения переменных

Важно: Используй уникальные email и phone для каждого теста регистрации!
```

---

## 📊 Анализ результатов предыдущего тестирования

### Статистика:
- ✅ Успешных тестов: 102
- ❌ Неудачных тестов: 135
- 🔴 Критичных ошибок: 2 (Register Step 1 для клиента и тренера - 500)
- 🟡 Ожидаемых ошибок: 133 (401 Unauthorized из-за неудачной регистрации)

### Основная проблема:
Register Step 1 возвращает 500 (Internal Server Error), что препятствует успешной регистрации и блокирует все последующие запросы, требующие авторизации.

### Решение:
1. Проверить логи сервера при выполнении Register Step 1
2. Убедиться, что база данных работает и таблицы созданы
3. Проверить корректность данных запроса
4. Исправить найденную проблему
5. Повторно протестировать полный флоу

---

## 📝 Как использовать

1. Скопируйте улучшенный промпт выше
2. Вставьте в AI Postman
3. AI начнет диагностику проблемы с 500 ошибкой
4. После исправления протестирует весь флоу
5. Создаст отчет о результатах

