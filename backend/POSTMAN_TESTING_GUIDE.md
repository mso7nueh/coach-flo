# Postman Testing Guide - Coach Fit API

Полное руководство по тестированию API через Postman с примерами JSON запросов и ответов.

## 🔑 Настройка Postman

### Переменные окружения
Создайте переменные в Postman:
- `base_url` = `http://localhost:8000` (или ваш URL)
- `client_token` = (будет заполнено после авторизации клиента)
- `trainer_token` = (будет заполнено после авторизации тренера)
- `client_id` = (ID клиента для тестирования)
- `trainer_id` = (ID тренера для тестирования)

### Заголовки
Для всех защищенных эндпоинтов добавьте заголовок:
```
Authorization: Bearer {{client_token}}
```
или
```
Authorization: Bearer {{trainer_token}}
```

---

## 👤 ФЛОУ ДЛЯ КЛИЕНТА

### 1. Регистрация клиента

> ⚠️ **ВАЖНО:** Для регистрации НЕ нужно вызывать `/send-sms` отдельно! Эндпоинт `/register/step1` автоматически отправляет SMS код.

#### Шаг 1: Регистрация (Step 1) - отправка данных и SMS кода
**POST** `{{base_url}}/api/auth/register/step1`

**Body (JSON):**
```json
{
  "full_name": "Иван Иванов",
  "email": "client@example.com",
  "password": "password123",
  "phone": "+7 (999) 123-45-67",
  "role": "client",
  "trainer_code": "TRAINER123"
}
```

**Параметры:**
- ✅ `full_name` - обязательный (минимум 2 символа)
- ✅ `email` - обязательный
- ✅ `password` - обязательный (минимум 6 символов)
- ✅ `phone` - обязательный
- ✅ `role` - обязательный (`client` или `trainer`)
- ❌ `trainer_code` - необязательный (код тренера для связи)

**Ответ:**
```json
{
  "verified": false,
  "message": "SMS код отправлен на ваш телефон"
}
```
> ⚠️ **В режиме разработки SMS код выводится в консоль сервера!** Проверьте консоль, где запущен сервер.

---

#### Шаг 2: Подтверждение SMS кода (Step 2) - завершение регистрации
**POST** `{{base_url}}/api/auth/register/step2`

**Body (JSON):**
```json
{
  "phone": "+7 (999) 123-45-67",
  "code": "1234"
}
```

**Параметры:**
- ✅ `phone` - обязательный (должен совпадать с телефоном из Step 1)
- ✅ `code` - обязательный (4 цифры, код из консоли сервера в режиме разработки)

> ⚠️ **ВАЖНО:** 
> - Используйте тот же телефон, что и в Step 1
> - Код действителен 10 минут
> - Если код истёк, начните регистрацию заново с Step 1

**Ответ:**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": "uuid-here",
    "full_name": "Иван Иванов",
    "email": "client@example.com",
    "phone": "+7 (999) 123-45-67",
    "role": "client",
    "onboarding_seen": false,
    "locale": "ru",
    "phone_verified": true,
    "created_at": "2024-01-01T12:00:00"
  },
  "requires_onboarding": true
}
```
> 💾 Сохраните `token` в переменную `{{client_token}}`

---

### 2. Отправка SMS кода (отдельно, не для регистрации)

> ⚠️ **Этот эндпоинт НЕ используется для регистрации!** Он нужен только для отдельной верификации телефона.

**POST** `{{base_url}}/api/auth/send-sms`

**Body (JSON):**
```json
{
  "phone": "+7 (999) 123-45-67"
}
```

**Ответ:**
```json
{
  "verified": false,
  "message": "SMS код отправлен"
}
```
> ⚠️ В режиме разработки код выводится в консоль сервера

---

### 2.1. Проверка SMS кода (отдельно, не для регистрации)

> ⚠️ **Этот эндпоинт НЕ используется для регистрации!** Он нужен только для отдельной верификации телефона.

**POST** `{{base_url}}/api/auth/verify-sms`

**Body (JSON):**
```json
{
  "phone": "+7 (999) 123-45-67",
  "code": "1234"
}
```

**Параметры:**
- ✅ `phone` - обязательный
- ✅ `code` - обязательный (4 цифры)

**Ответ:**
```json
{
  "verified": true,
  "message": "Телефон подтвержден"
}
```

> ⚠️ Код действителен 10 минут

---

### 3. Авторизация (альтернатива регистрации)

**POST** `{{base_url}}/api/auth/login`

**Body (JSON):**
```json
{
  "email": "client@example.com",
  "password": "password123"
}
```

**Параметры:**
- ✅ `email` - обязательный
- ✅ `password` - обязательный

**Ответ:** (аналогичен Step 2 регистрации)

---

### 4. Получение информации о себе

**GET** `{{base_url}}/api/users/me`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Ответ:**
```json
{
  "id": "uuid-here",
  "full_name": "Иван Иванов",
  "email": "client@example.com",
  "phone": "+7 (999) 123-45-67",
  "role": "client",
  "onboarding_seen": false,
  "locale": "ru",
  "phone_verified": true,
  "created_at": "2024-01-01T12:00:00",
  "trainer": {
    "id": "trainer-uuid",
    "full_name": "Алексей Тренеров",
    "email": "trainer@example.com",
    "phone": "+7 (999) 888-77-66",
    "role": "trainer",
    "onboarding_seen": true,
    "locale": "ru",
    "phone_verified": true,
    "created_at": "2024-01-01T10:00:00"
  }
}
```

> 💡 **Примечание:** Поле `trainer` присутствует только если клиент связан с тренером. Если тренера нет, поле будет `null`.

---

### 5. Онбординг

#### Завершение онбординга
**POST** `{{base_url}}/api/onboarding/complete`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Body (JSON):**
```json
{
  "weight": 75.5,
  "height": 180,
  "age": 30,
  "goals": ["weight_loss", "muscle_gain"],
  "restrictions": ["no_dairy", "vegetarian"],
  "activity_level": "medium"
}
```

**Параметры:**
- ❌ `weight` - необязательный (30-200 кг)
- ❌ `height` - необязательный (100-250 см)
- ❌ `age` - необязательный (14-100 лет)
- ❌ `goals` - необязательный (массив строк: `weight_loss`, `muscle_gain`, `endurance`, `strength`, `flexibility`, `general_fitness`)
- ❌ `restrictions` - необязательный (массив строк)
- ❌ `activity_level` - необязательный (`low`, `medium`, `high`)

**Ответ:**
```json
{
  "id": "onboarding-uuid",
  "user_id": "user-uuid",
  "weight": 75.5,
  "height": 180,
  "age": 30,
  "goals": ["weight_loss", "muscle_gain"],
  "restrictions": ["no_dairy", "vegetarian"],
  "activity_level": "medium",
  "created_at": "2024-01-01T12:00:00"
}
```

---

#### Обновление онбординга
**PUT** `{{base_url}}/api/onboarding/`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Body (JSON):**
```json
{
  "weight": 77.0,
  "goals": ["weight_loss", "endurance"]
}
```
> Можно обновить только нужные поля

---

#### Получение онбординга
**GET** `{{base_url}}/api/onboarding/`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Ответ:** (аналогичен ответу завершения онбординга)

---

### 6. Обновление профиля

**PUT** `{{base_url}}/api/users/me`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Body (JSON):**
```json
{
  "full_name": "Иван Петров",
  "email": "newemail@example.com",
  "phone": "+7 (999) 999-99-99",
  "avatar": "https://example.com/avatar.jpg",
  "locale": "en"
}
```

**Параметры:**
- ❌ Все поля необязательные (обновляются только переданные)

**Ответ:** (обновленный UserResponse)

---

### 7. Настройки

#### Получение настроек
**GET** `{{base_url}}/api/users/me/settings`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Ответ:**
```json
{
  "locale": "ru",
  "notificationSettings": {
    "emailEnabled": true,
    "pushEnabled": true,
    "smsEnabled": false,
    "reminderBeforeMinutes": 30,
    "workoutReminders": true,
    "workoutScheduled": true,
    "workoutCompleted": false,
    "metricsUpdate": false,
    "trainerNote": true
  }
}
```

---

#### Обновление настроек
**PUT** `{{base_url}}/api/users/me/settings`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Body (JSON):**
```json
{
  "locale": "en",
  "notificationSettings": {
    "emailEnabled": true,
    "pushEnabled": false,
    "smsEnabled": true,
    "reminderBeforeMinutes": 60,
    "workoutReminders": true,
    "workoutScheduled": true,
    "workoutCompleted": true,
    "metricsUpdate": true,
    "trainerNote": true
  }
}
```

**Параметры:**
- ✅ `locale` - обязательный (`ru` или `en`)
- ✅ `notificationSettings` - обязательный объект

---

### 8. Связь с тренером

#### Связать с тренером по коду
**POST** `{{base_url}}/api/users/link-trainer`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Body (JSON):**
```json
{
  "connection_code": "TRAINER123"
}
```

**Параметры:**
- ✅ `connection_code` - обязательный

---

#### Отвязать от тренера
**POST** `{{base_url}}/api/users/unlink-trainer`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Body:** (пустой)

---

### 9. Тренировки

#### Создание тренировки
**POST** `{{base_url}}/api/workouts`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Body (JSON):**
```json
{
  "title": "Утренняя пробежка",
  "start": "2024-01-15T08:00:00",
  "end": "2024-01-15T09:00:00",
  "location": "Парк Горького",
  "format": "offline",
  "trainer_id": "trainer-uuid",
  "program_day_id": "day-uuid",
  "recurrence_frequency": "weekly",
  "recurrence_interval": 1,
  "recurrence_days_of_week": [1, 3, 5],
  "recurrence_end_date": "2024-03-15T09:00:00",
  "recurrence_occurrences": 12
}
```

**Параметры:**
- ✅ `title` - обязательный
- ✅ `start` - обязательный (ISO 8601)
- ✅ `end` - обязательный (ISO 8601)
- ❌ `location` - необязательный
- ❌ `format` - необязательный (`online`, `offline`)
- ❌ `trainer_id` - необязательный (ID тренера, если тренировка с тренером)
- ❌ `program_day_id` - необязательный
- ❌ `recurrence_frequency` - необязательный (`daily`, `weekly`, `monthly`)
- ❌ `recurrence_interval` - необязательный (интервал повторения)
- ❌ `recurrence_days_of_week` - необязательный (массив: 0=понедельник, 6=воскресенье)
- ❌ `recurrence_end_date` - необязательный
- ❌ `recurrence_occurrences` - необязательный (количество повторений)

**Ответ:**
```json
{
  "id": "workout-uuid",
  "user_id": "user-uuid",
  "title": "Утренняя пробежка",
  "start": "2024-01-15T08:00:00",
  "end": "2024-01-15T09:00:00",
  "location": "Парк Горького",
  "format": "offline",
  "attendance": "scheduled",
  "created_at": "2024-01-01T12:00:00"
}
```

---

#### Получение списка тренировок
**GET** `{{base_url}}/api/workouts?start_date=2024-01-01T00:00:00&end_date=2024-01-31T23:59:59`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Query параметры:**
- ❌ `start_date` - необязательный (ISO 8601)
- ❌ `end_date` - необязательный (ISO 8601)

**Ответ:** (массив WorkoutResponse)

---

#### Получение тренировки по ID
**GET** `{{base_url}}/api/workouts/{workout_id}`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

---

#### Обновление тренировки
**PUT** `{{base_url}}/api/workouts/{workout_id}`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Body (JSON):**
```json
{
  "title": "Обновленная тренировка",
  "start": "2024-01-15T09:00:00",
  "end": "2024-01-15T10:00:00",
  "attendance": "completed",
  "coach_note": "Отличная тренировка!"
}
```

**Параметры:**
- ❌ Все поля необязательные

---

#### Удаление тренировки
**DELETE** `{{base_url}}/api/workouts/{workout_id}?delete_series=false`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Query параметры:**
- ❌ `delete_series` - необязательный (`true` для удаления всей серии повторяющихся тренировок)

---

### 10. Программы тренировок

#### Создание программы
**POST** `{{base_url}}/api/programs`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Body (JSON):**
```json
{
  "title": "Программа для похудения",
  "description": "Интенсивная программа на 12 недель"
}
```

**Параметры:**
- ✅ `title` - обязательный
- ❌ `description` - необязательный

---

#### Получение списка программ
**GET** `{{base_url}}/api/programs`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Ответ:** (массив TrainingProgramResponse)

---

#### Получение программы по ID
**GET** `{{base_url}}/api/programs/{program_id}`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Ответ:** (TrainingProgramResponse)

> 💡 Клиент может просматривать только свои программы

---

#### Создание дня программы
**POST** `{{base_url}}/api/programs/{program_id}/days`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Body (JSON):**
```json
{
  "name": "День 1: Верх тела",
  "notes": "Фокус на грудь и спину",
  "blocks": [
    {
      "type": "warmup",
      "title": "Разминка",
      "exercises": [
        {
          "title": "Беговая дорожка",
          "sets": 1,
          "duration": "10 минут"
        }
      ]
    },
    {
      "type": "main",
      "title": "Основная часть",
      "exercises": [
        {
          "title": "Жим лежа",
          "sets": 4,
          "reps": 10,
          "weight": "80 кг",
          "rest": "2 минуты"
        }
      ]
    },
    {
      "type": "cooldown",
      "title": "Заминка",
      "exercises": [
        {
          "title": "Растяжка",
          "sets": 1,
          "duration": "15 минут"
        }
      ]
    }
  ]
}
```

**Параметры:**
- ✅ `name` - обязательный
- ❌ `notes` - необязательный
- ❌ `blocks` - необязательный (массив блоков)
- ❌ `source_template_id` - необязательный

---

### 11. Метрики тела

#### Создание метрики
**POST** `{{base_url}}/api/metrics/body`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Body (JSON):**
```json
{
  "label": "Вес",
  "unit": "кг",
  "target": 70.0
}
```

**Параметры:**
- ✅ `label` - обязательный
- ✅ `unit` - обязательный
- ❌ `target` - необязательный

---

#### Добавление записи метрики
**POST** `{{base_url}}/api/metrics/body/entries`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Body (JSON):**
```json
{
  "metric_id": "metric-uuid",
  "value": 75.5,
  "recorded_at": "2024-01-15T08:00:00"
}
```

**Параметры:**
- ✅ `metric_id` - обязательный
- ✅ `value` - обязательный
- ✅ `recorded_at` - обязательный (ISO 8601)

---

#### Получение записей метрик
**GET** `{{base_url}}/api/metrics/body/entries?metric_id={metric_id}&start_date=2024-01-01&end_date=2024-01-31`

**Query параметры:**
- ❌ `metric_id` - необязательный
- ❌ `start_date` - необязательный
- ❌ `end_date` - необязательный

---

### 12. Метрики упражнений

#### Создание метрики упражнения
**POST** `{{base_url}}/api/metrics/exercise`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Body (JSON):**
```json
{
  "label": "Жим лежа",
  "muscle_group": "Грудь"
}
```

**Параметры:**
- ✅ `label` - обязательный
- ❌ `muscle_group` - необязательный

---

#### Добавление записи метрики упражнения
**POST** `{{base_url}}/api/metrics/exercise/entries`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Body (JSON):**
```json
{
  "exercise_metric_id": "exercise-metric-uuid",
  "date": "2024-01-15T08:00:00",
  "weight": 80.0,
  "repetitions": 10,
  "sets": 4
}
```

**Параметры:**
- ✅ `exercise_metric_id` - обязательный
- ✅ `date` - обязательный (ISO 8601)
- ❌ `weight` - необязательный
- ❌ `repetitions` - необязательный
- ❌ `sets` - необязательный

---

### 13. Питание

#### Создание/обновление записи питания
**POST** `{{base_url}}/api/nutrition/`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Body (JSON):**
```json
{
  "date": "2024-01-15T00:00:00",
  "calories": 2000,
  "proteins": 150,
  "fats": 60,
  "carbs": 250,
  "notes": "Завтрак: овсянка с фруктами"
}
```

**Параметры:**
- ✅ `date` - обязательный (ISO 8601)
- ✅ `calories` - обязательный
- ❌ `proteins` - необязательный
- ❌ `fats` - необязательный
- ❌ `carbs` - необязательный
- ❌ `notes` - необязательный

> ⚠️ Если запись на эту дату уже существует, она будет обновлена

---

#### Получение записей питания
**GET** `{{base_url}}/api/nutrition/?start_date=2024-01-01&end_date=2024-01-31`

**Query параметры:**
- ❌ `start_date` - необязательный
- ❌ `end_date` - необязательный

---

#### Получение записи питания по ID
**GET** `{{base_url}}/api/nutrition/{entry_id}`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

---

#### Обновление записи питания
**PUT** `{{base_url}}/api/nutrition/{entry_id}`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Body (JSON):**
```json
{
  "date": "2024-01-15T00:00:00",
  "calories": 2200,
  "proteins": 160,
  "fats": 70,
  "carbs": 280,
  "notes": "Обновленные данные"
}
```

**Параметры:**
- ✅ `date` - обязательный
- ✅ `calories` - обязательный
- ❌ `proteins` - необязательный
- ❌ `fats` - необязательный
- ❌ `carbs` - необязательный
- ❌ `notes` - необязательный

---

#### Удаление записи питания
**DELETE** `{{base_url}}/api/nutrition/{entry_id}`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

---

### 14. Дашборд

**GET** `{{base_url}}/api/dashboard/stats?period=7d`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Query параметры:**
- ❌ `period` - необязательный (`7d`, `14d`, `30d`)

**Ответ:**
```json
{
  "total_workouts": 20,
  "completed_workouts": 18,
  "attendance_rate": 0.9,
  "today_workouts": 1,
  "next_workout": {
    "id": "workout-uuid",
    "title": "Утренняя пробежка",
    "start": "2024-01-15T08:00:00",
    "end": "2024-01-15T09:00:00"
  }
}
```

---

### 15. Заметки от тренера

**GET** `{{base_url}}/api/notes`

**Headers:**
```
Authorization: Bearer {{client_token}}
```

**Ответ:** (массив заметок от тренера)

---

## 🏋️ ФЛОУ ДЛЯ ТРЕНЕРА

### 1. Регистрация тренера

> ⚠️ **ВАЖНО:** Для регистрации НЕ нужно вызывать `/send-sms` отдельно! Эндпоинт `/register/step1` автоматически отправляет SMS код.

#### Шаг 1: Регистрация (Step 1) - отправка данных и SMS кода
**POST** `{{base_url}}/api/auth/register/step1`

**Body (JSON):**
```json
{
  "full_name": "Алексей Тренеров",
  "email": "trainer@example.com",
  "password": "password123",
  "phone": "+7 (999) 888-77-66",
  "role": "trainer"
}
```

> ⚠️ Для тренера `trainer_code` не нужен

**Ответ:**
```json
{
  "verified": false,
  "message": "SMS код отправлен на ваш телефон"
}
```
> ⚠️ **В режиме разработки SMS код выводится в консоль сервера!** Проверьте консоль, где запущен сервер.

---

#### Шаг 2: Подтверждение SMS кода (Step 2) - завершение регистрации
**POST** `{{base_url}}/api/auth/register/step2`

**Body (JSON):**
```json
{
  "phone": "+7 (999) 888-77-66",
  "code": "1234"
}
```

> 💾 Сохраните `token` в переменную `{{trainer_token}}`
> 💾 Сохраните `trainer_connection_code` из ответа для связи клиентов
> ⚠️ **ВАЖНО:** Используйте тот же телефон, что и в Step 1, и код из консоли сервера

---

### 2. Управление клиентами

#### Получение списка клиентов
**GET** `{{base_url}}/api/clients?search=Иван`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

**Query параметры:**
- ❌ `search` - необязательный (поиск по имени)

**Ответ:** (массив UserResponse)

---

#### Добавление клиента
**POST** `{{base_url}}/api/clients`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

**Body (JSON):**
```json
{
  "full_name": "Новый Клиент",
  "email": "newclient@example.com",
  "password": "password123",
  "phone": "+7 (999) 111-22-33",
  "role": "client"
}
```

**Параметры:**
- ✅ `full_name` - обязательный
- ✅ `email` - обязательный
- ✅ `password` - обязательный (минимум 6 символов)
- ✅ `role` - обязательный (должен быть `client`)
- ❌ `phone` - необязательный

> 💾 Сохраните `id` из ответа в переменную `{{client_id}}`

---

#### Получение информации о клиенте
**GET** `{{base_url}}/api/clients/{{client_id}}`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

---

#### Обновление данных клиента
**PUT** `{{base_url}}/api/clients/{{client_id}}`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

**Body (JSON):**
```json
{
  "full_name": "Обновленное Имя",
  "email": "updated@example.com",
  "client_format": "both",
  "workouts_package": 12,
  "package_expiry_date": "2024-12-31T23:59:59",
  "is_active": true,
  "weight": 75.5,
  "height": 180,
  "age": 30,
  "goals": ["weight_loss", "muscle_gain"],
  "restrictions": ["no_dairy"],
  "activity_level": "high"
}
```

**Параметры:**
- ❌ Все поля необязательные
- Можно обновить основные данные, данные клиента и онбординг одновременно

---

#### Получение статистики клиента
**GET** `{{base_url}}/api/clients/{{client_id}}/stats`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

**Ответ:**
```json
{
  "total_workouts": 20,
  "completed_workouts": 18,
  "attendance_rate": 0.9,
  "last_workout": "2024-01-10T10:00:00",
  "next_workout": "2024-01-15T08:00:00"
}
```

---

#### Получение онбординга клиента
**GET** `{{base_url}}/api/clients/{{client_id}}/onboarding`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

**Ответ:** (OnboardingResponse)

---

### 3. Создание тренировок для клиентов

#### Создание тренировки для клиента
**POST** `{{base_url}}/api/workouts`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

**Body (JSON):**
```json
{
  "title": "Силовая тренировка",
  "start": "2024-01-15T10:00:00",
  "end": "2024-01-15T11:30:00",
  "location": "Зал №2",
  "format": "offline",
  "user_id": "{{client_id}}",
  "program_day_id": "day-uuid"
}
```

**Параметры:**
- ✅ `title` - обязательный
- ✅ `start` - обязательный
- ✅ `end` - обязательный
- ✅ `user_id` - обязательный для тренера (ID клиента)
- ❌ `location` - необязательный
- ❌ `format` - необязательный
- ❌ `program_day_id` - необязательный

> ⚠️ Для тренера `user_id` или `trainer_id` должен содержать ID клиента

---

#### Просмотр всех тренировок команды
**GET** `{{base_url}}/api/workouts?trainer_view=true&start_date=2024-01-01&end_date=2024-01-31`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

**Query параметры:**
- ❌ `trainer_view` - необязательный (`true` для просмотра всех тренировок команды)
- ❌ `client_id` - необязательный (для фильтрации по клиенту)
- ❌ `start_date` - необязательный
- ❌ `end_date` - необязательный

---

#### Просмотр тренировок конкретного клиента
**GET** `{{base_url}}/api/workouts?client_id={{client_id}}&start_date=2024-01-01&end_date=2024-01-31`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

---

### 4. Просмотр программ клиента

#### Получение программ клиента
**GET** `{{base_url}}/api/programs?user_id={{client_id}}`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

**Query параметры:**
- ❌ `user_id` - необязательный (ID клиента для просмотра его программ)

---

#### Просмотр программы клиента
**GET** `{{base_url}}/api/programs/{program_id}`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

> 💡 Также доступно для клиентов для просмотра своих программ

---

#### Получение дня программы по ID
**GET** `{{base_url}}/api/programs/{program_id}/days/{day_id}`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```
или
```
Authorization: Bearer {{client_token}}
```

> 💡 Тренер может просматривать дни программ своих клиентов, клиент - только свои программы

---

#### Удаление программы
**DELETE** `{{base_url}}/api/programs/{program_id}`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```
или
```
Authorization: Bearer {{client_token}}
```

> ⚠️ Тренер может удалять только свои программы (не программы клиентов). Клиент может удалять только свои программы.

---

### 5. Просмотр метрик клиента

#### Метрики тела клиента
**GET** `{{base_url}}/api/metrics/body?user_id={{client_id}}`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

**Query параметры:**
- ❌ `user_id` - необязательный (ID клиента)

---

#### Записи метрик тела клиента
**GET** `{{base_url}}/api/metrics/body/entries?user_id={{client_id}}&metric_id={metric_id}`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

---

#### Метрики упражнений клиента
**GET** `{{base_url}}/api/metrics/exercise?user_id={{client_id}}`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

---

### 6. Финансы

#### Создание платежа
**POST** `{{base_url}}/api/finances`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

**Body (JSON):**
```json
{
  "client_id": "{{client_id}}",
  "amount": 5000,
  "date": "2024-01-15T00:00:00",
  "type": "package",
  "package_size": 12,
  "subscription_days": 30,
  "notes": "Пакет на 12 тренировок"
}
```

**Параметры:**
- ✅ `client_id` - обязательный
- ✅ `amount` - обязательный
- ✅ `date` - обязательный (ISO 8601)
- ✅ `type` - обязательный (`package`, `subscription`, `single`)
- ❌ `package_size` - необязательный (количество тренировок в пакете)
- ❌ `subscription_days` - необязательный (дней подписки)
- ❌ `notes` - необязательный

---

#### Получение списка платежей
**GET** `{{base_url}}/api/finances?client_id={{client_id}}&start_date=2024-01-01&end_date=2024-01-31`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

**Query параметры:**
- ❌ `client_id` - необязательный
- ❌ `start_date` - необязательный
- ❌ `end_date` - необязательный

---

#### Статистика по финансам
**GET** `{{base_url}}/api/finances/stats`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

**Ответ:**
```json
{
  "total_revenue": 150000,
  "active_clients": 10,
  "total_packages": 50,
  "monthly_revenue": 50000
}
```

---

#### Удаление платежа
**DELETE** `{{base_url}}/api/finances/{payment_id}`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

> ⚠️ Только тренер может удалять платежи

---

### 7. Библиотека упражнений

#### Создание упражнения
**POST** `{{base_url}}/api/exercises`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

**Body (JSON):**
```json
{
  "name": "Жим лежа",
  "description": "Базовое упражнение для развития груди",
  "muscle_groups": "Грудь, Трицепс",
  "equipment": "Штанга, Скамья",
  "difficulty": "intermediate"
}
```

**Параметры:**
- ✅ `name` - обязательный
- ❌ `description` - необязательный
- ❌ `muscle_groups` - необязательный
- ❌ `equipment` - необязательный
- ❌ `difficulty` - необязательный

---

#### Получение списка упражнений
**GET** `{{base_url}}/api/exercises?search=жим&muscle_group=Грудь`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

**Query параметры:**
- ❌ `search` - необязательный
- ❌ `muscle_group` - необязательный

---

#### Получение упражнения по ID
**GET** `{{base_url}}/api/exercises/{exercise_id}`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

---

#### Обновление упражнения
**PUT** `{{base_url}}/api/exercises/{exercise_id}`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

**Body (JSON):**
```json
{
  "name": "Жим лежа (обновлено)",
  "description": "Обновленное описание",
  "muscle_groups": "Грудь, Трицепс, Плечи",
  "equipment": "Штанга, Скамья",
  "difficulty": "advanced"
}
```

**Параметры:**
- ✅ `name` - обязательный
- ❌ `description` - необязательный
- ❌ `muscle_groups` - необязательный
- ❌ `equipment` - необязательный
- ❌ `difficulty` - необязательный

> ⚠️ Тренер может обновлять только свои упражнения

---

#### Удаление упражнения
**DELETE** `{{base_url}}/api/exercises/{exercise_id}`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

> ⚠️ Тренер может удалять только свои упражнения

---

### 8. Заметки для клиентов

#### Создание заметки
**POST** `{{base_url}}/api/notes`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

**Body (JSON):**
```json
{
  "client_id": "{{client_id}}",
  "title": "Прогресс за неделю",
  "content": "Отличный прогресс в жиме лежа. Увеличил вес на 5 кг."
}
```

**Параметры:**
- ✅ `client_id` - обязательный
- ✅ `title` - обязательный
- ❌ `content` - необязательный

---

#### Получение заметок
**GET** `{{base_url}}/api/notes?client_id={{client_id}}`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

**Query параметры:**
- ❌ `client_id` - необязательный (для фильтрации по клиенту)

> 💡 Клиенты также могут использовать этот эндпоинт для просмотра заметок от своего тренера

---

#### Получение заметки по ID
**GET** `{{base_url}}/api/notes/{note_id}`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```
или
```
Authorization: Bearer {{client_token}}
```

> 💡 Тренер видит свои заметки, клиент - заметки от своего тренера

---

#### Обновление заметки
**PUT** `{{base_url}}/api/notes/{note_id}`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

**Body (JSON):**
```json
{
  "title": "Обновленный заголовок",
  "content": "Обновленное содержание"
}
```

**Параметры:**
- ❌ Все поля необязательные

---

#### Удаление заметки
**DELETE** `{{base_url}}/api/notes/{note_id}`

**Headers:**
```
Authorization: Bearer {{trainer_token}}
```

---

## 📝 Общие замечания

### ⚠️ Важно: Правильная последовательность регистрации

**Для регистрации используйте ТОЛЬКО:**
1. `POST /api/auth/register/step1` - автоматически отправляет SMS код
2. `POST /api/auth/register/step2` - подтверждает код и создает пользователя

**НЕ вызывайте `/send-sms` перед регистрацией!** Это вызовет проблемы:
- Код от `/send-sms` не связан с процессом регистрации
- `step1` отправит новый код, а вы будете использовать старый
- Код может истечь или быть неверным

**Эндпоинт `/send-sms` используется только для:**
- Отдельной верификации телефона (не для регистрации)
- Восстановления доступа
- Других операций, не связанных с регистрацией

### Форматы дат
Все даты должны быть в формате ISO 8601:
```
2024-01-15T08:00:00
2024-01-15T08:00:00Z
2024-01-15T08:00:00+03:00
```

### Коды ответов
- `200` - Успешный запрос
- `201` - Ресурс создан
- `204` - Успешное удаление (без тела ответа)
- `400` - Ошибка валидации
- `401` - Не авторизован
- `403` - Нет доступа
- `404` - Ресурс не найден
- `500` - Внутренняя ошибка сервера

### Обработка ошибок
Все ошибки возвращаются в формате:
```json
{
  "detail": "Описание ошибки"
}
```

### Тестирование повторяющихся тренировок
При создании тренировки с `recurrence_frequency` система автоматически создаст все повторяющиеся тренировки согласно правилам.

### Тестирование связи клиента с тренером
1. Тренер регистрируется и получает `trainer_connection_code`
2. Клиент регистрируется с `trainer_code` или использует `POST /api/users/link-trainer`

---

## 🚀 Быстрый старт для тестирования

### Для клиента:
1. Регистрация (`POST /api/auth/register/step1` → `POST /api/auth/register/step2`)
   - ⚠️ НЕ вызывайте `/send-sms` перед регистрацией!
   - Step 1 автоматически отправит SMS код
   - Проверьте консоль сервера для получения кода
2. Онбординг
3. Создание тренировки
4. Добавление метрик
5. Просмотр дашборда

### Для тренера:
1. Регистрация (`POST /api/auth/register/step1` → `POST /api/auth/register/step2`)
   - ⚠️ НЕ вызывайте `/send-sms` перед регистрацией!
   - Step 1 автоматически отправит SMS код
   - Проверьте консоль сервера для получения кода
   - Сохраните `trainer_connection_code` из ответа Step 2
2. Добавление клиента
3. Создание тренировки для клиента
4. Просмотр метрик клиента
5. Создание платежа
6. Добавление заметки для клиента

