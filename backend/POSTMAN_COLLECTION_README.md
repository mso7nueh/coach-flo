# Postman Collection для Coach Fit API

## 📦 Файл коллекции

Файл `Coach_Fit_API.postman_collection.json` содержит полную коллекцию всех эндпоинтов API в формате Postman Collection v2.1.

## 🚀 Как использовать с AI Postman

### Вариант 1: Импорт в Postman

1. Откройте Postman (или используйте веб-версию на postman.com)
2. Нажмите **Import** (Импорт)
3. Выберите файл `Coach_Fit_API.postman_collection.json`
4. Коллекция будет импортирована со всеми запросами и папками

### Вариант 2: Использование с AI Postman

Если вы используете AI-функции Postman (например, Postman AI или другие AI-инструменты):

1. Импортируйте коллекцию в Postman (см. Вариант 1)
2. Убедитесь, что переменные окружения установлены:
   - `base_url` = `http://localhost:8000` (или ваш URL сервера)
   - `client_token` = (заполнится автоматически после авторизации)
   - `trainer_token` = (заполнится автоматически после авторизации)
   - `client_id` = (заполнится автоматически при создании клиента)
   - `trainer_id` = (заполнится автоматически при регистрации тренера)

3. Используйте AI-функции Postman для:
   - Генерации тестов
   - Создания документации
   - Оптимизации запросов
   - Анализа ответов

### Автоматическое сохранение токенов

Некоторые запросы автоматически сохраняют токены и ID в переменные коллекции:
- **Register Client Step 2** → сохраняет `client_token` и `client_id`
- **Register Trainer Step 2** → сохраняет `trainer_token` и `trainer_id`
- **Login** → сохраняет токен в зависимости от роли пользователя
- **Add Client** → сохраняет `client_id`

## 📁 Структура коллекции

Коллекция организована по папкам:

1. **🔐 Auth - Аутентификация**
   - Регистрация клиента и тренера (Step 1, Step 2)
   - Авторизация (Login)
   - SMS верификация (не для регистрации)
   - Получение текущего пользователя

2. **👤 Users - Пользователи**
   - Получение и обновление профиля
   - Связь с тренером

3. **⚙️ Settings - Настройки**
   - Получение и обновление настроек

4. **🎯 Onboarding - Онбординг**
   - Завершение онбординга
   - Получение и обновление данных онбординга

5. **🏋️ Workouts - Тренировки**
   - Создание, получение, обновление, удаление тренировок
   - Работа с повторяющимися тренировками
   - Запросы для тренера

6. **📋 Programs - Программы тренировок**
   - Управление программами
   - Работа с днями программ

7. **📊 Metrics - Метрики**
   - Метрики тела (Body Metrics)
   - Метрики упражнений (Exercise Metrics)

8. **🍎 Nutrition - Питание**
   - Записи о питании

9. **📈 Dashboard - Дашборд**
   - Статистика

10. **👥 Clients - Клиенты (для тренера)**
    - Управление клиентами
    - Статистика клиентов

11. **💰 Finances - Финансы (для тренера)**
    - Платежи
    - Статистика по финансам

12. **📚 Exercises - Библиотека упражнений (для тренера)**
    - Управление упражнениями

13. **📝 Notes - Заметки**
    - Заметки тренера для клиентов

## 🔧 Настройка переменных

### В Postman:

1. Откройте коллекцию
2. Перейдите на вкладку **Variables** (Переменные)
3. Установите `base_url` = `http://localhost:8000` (или ваш URL)

Остальные переменные (`client_token`, `trainer_token`, `client_id`, `trainer_id`) заполняются автоматически при выполнении соответствующих запросов.

### В коде запросов:

Все запросы используют переменные коллекции через синтаксис `{{variable_name}}`, например:
- `{{base_url}}/api/auth/login`
- `Bearer {{client_token}}`

## ⚠️ Важные замечания

1. **Регистрация**: Используйте только `/register/step1` → `/register/step2`. НЕ вызывайте `/send-sms` перед регистрацией!

2. **SMS коды**: В режиме разработки SMS коды выводятся в консоль сервера

3. **Токены**: Токены автоматически сохраняются после успешной регистрации/авторизации

4. **Даты**: Используйте формат ISO 8601: `2024-01-15T08:00:00`

## 📖 Дополнительная документация

Для подробной информации о каждом эндпоинте см. файл `POSTMAN_TESTING_GUIDE.md`

