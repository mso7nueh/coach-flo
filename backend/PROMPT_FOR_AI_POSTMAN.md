# Промпт для AI Postman

Скопируйте и вставьте этот промпт в AI Postman для автоматического тестирования всей коллекции API.

---

## 📋 Промпт для тестирования

```
Пожалуйста, протестируй всю Postman коллекцию "Coach Fit API" следующим образом:

## Задачи для выполнения:

### 1. Анализ коллекции
- Проверь все эндпоинты в коллекции на корректность URL, методов запросов и структуру JSON
- Убедись, что все запросы используют правильные заголовки (Content-Type, Authorization)
- Проверь наличие всех обязательных полей в телах запросов

### 2. Создай тестовые сценарии для полного флоу

#### Флоу для клиента:
1. Регистрация клиента:
   - POST /api/auth/register/step1 (с данными клиента)
   - Проверь ответ, убедись что SMS код отправлен (в консоли сервера)
   - POST /api/auth/register/step2 (с кодом из консоли сервера)
   - Проверь, что токен сохранен в переменную client_token
   - Проверь, что user.id сохранен в переменную client_id

2. Онбординг:
   - POST /api/onboarding/complete (с данными онбординга)
   - GET /api/onboarding/ (проверь получение данных)

3. Профиль:
   - GET /api/users/me (проверь получение профиля)
   - PUT /api/users/me (обнови профиль)
   - GET /api/users/me/settings (получи настройки)
   - PUT /api/users/me/settings (обнови настройки)

4. Тренировки:
   - POST /api/workouts (создай тренировку)
   - GET /api/workouts (получи список тренировок)
   - PUT /api/workouts/{workout_id} (обнови тренировку)
   - DELETE /api/workouts/{workout_id} (удали тренировку)

5. Программы:
   - POST /api/programs (создай программу)
   - GET /api/programs (получи список программ)
   - POST /api/programs/{program_id}/days (создай день программы)
   - GET /api/programs/{program_id}/days/{day_id} (получи день программы)

6. Метрики:
   - POST /api/metrics/body (создай метрику тела)
   - POST /api/metrics/body/entries (добавь запись метрики)
   - POST /api/metrics/exercise (создай метрику упражнения)
   - POST /api/metrics/exercise/entries (добавь запись метрики упражнения)

7. Питание:
   - POST /api/nutrition/ (создай запись о питании)
   - GET /api/nutrition/ (получи записи)
   - PUT /api/nutrition/{entry_id} (обнови запись)

8. Дашборд:
   - GET /api/dashboard/stats (получи статистику)

#### Флоу для тренера:
1. Регистрация тренера:
   - POST /api/auth/register/step1 (с данными тренера, role: trainer)
   - POST /api/auth/register/step2 (с кодом из консоли сервера)
   - Проверь, что токен сохранен в переменную trainer_token
   - Сохрани trainer_connection_code для связи с клиентом

2. Управление клиентами:
   - POST /api/clients (добавь клиента)
   - Проверь, что client_id сохранен в переменную
   - GET /api/clients (получи список клиентов)
   - GET /api/clients/{client_id} (получи данные клиента)
   - PUT /api/clients/{client_id} (обнови данные клиента)
   - GET /api/clients/{client_id}/stats (получи статистику клиента)
   - GET /api/clients/{client_id}/onboarding (получи онбординг клиента)

3. Тренировки для клиентов:
   - POST /api/workouts (создай тренировку для клиента с user_id)
   - GET /api/workouts?trainer_view=true&client_id={client_id} (получи все тренировки команды)

4. Программы клиентов:
   - GET /api/programs?user_id={client_id} (получи программы клиента)

5. Метрики клиентов:
   - GET /api/metrics/body?user_id={client_id} (метрики тела клиента)
   - GET /api/metrics/exercise?user_id={client_id} (метрики упражнений клиента)

6. Финансы:
   - POST /api/finances (создай платеж для клиента)
   - GET /api/finances (получи список платежей)
   - GET /api/finances/stats (получи статистику по финансам)
   - DELETE /api/finances/{payment_id} (удали платеж)

7. Библиотека упражнений:
   - POST /api/exercises (создай упражнение)
   - GET /api/exercises (получи список упражнений)
   - PUT /api/exercises/{exercise_id} (обнови упражнение)
   - DELETE /api/exercises/{exercise_id} (удали упражнение)

8. Заметки:
   - POST /api/notes (создай заметку для клиента)
   - GET /api/notes?client_id={client_id} (получи заметки)
   - PUT /api/notes/{note_id} (обнови заметку)
   - DELETE /api/notes/{note_id} (удали заметку)

### 3. Проверки для каждого запроса:

Для каждого запроса выполни следующие проверки:

1. **Валидация запроса:**
   - Проверь корректность JSON тела запроса
   - Проверь наличие всех обязательных полей
   - Проверь типы данных (строки, числа, массивы, объекты)
   - Проверь форматы (email, даты ISO 8601, телефоны)

2. **Валидация ответа:**
   - Проверь статус код ответа (200, 201, 204, 400, 401, 403, 404, 500)
   - Проверь структуру JSON ответа
   - Проверь наличие ожидаемых полей в ответе
   - Проверь типы данных в ответе

3. **Автоматическое сохранение переменных:**
   - После успешной регистрации/авторизации проверь сохранение токенов
   - После создания ресурсов проверь сохранение ID в переменные
   - Проверь использование переменных в последующих запросах

4. **Обработка ошибок:**
   - Проверь обработку неверных данных (400 Bad Request)
   - Проверь обработку неавторизованных запросов (401 Unauthorized)
   - Проверь обработку недостающих прав доступа (403 Forbidden)
   - Проверь обработку несуществующих ресурсов (404 Not Found)

5. **Тестовые скрипты:**
   - Создай тестовые скрипты для каждого запроса:
     * Проверка статус кода
     * Проверка структуры ответа
     * Проверка типов данных
     * Сохранение переменных (если необходимо)

### 4. Создай отчет о тестировании:

После выполнения всех тестов создай отчет со следующими разделами:

1. **Общая статистика:**
   - Количество протестированных эндпоинтов
   - Количество успешных запросов
   - Количество ошибок
   - Покрытие API (%)

2. **Найденные проблемы:**
   - Список всех ошибок с описанием
   - Некорректные эндпоинты
   - Проблемы с валидацией
   - Проблемы с авторизацией

3. **Рекомендации:**
   - Предложения по улучшению API
   - Предложения по улучшению коллекции
   - Предложения по добавлению тестов

4. **Полные флоу:**
   - Клиентский флоу: регистрация → онбординг → использование API
   - Тренерский флоу: регистрация → управление клиентами → использование API

### 5. Важные замечания:

⚠️ **Регистрация:**
- НЕ используй /send-sms перед регистрацией!
- Используй только /register/step1 → /register/step2
- SMS коды выводятся в консоль сервера (в режиме разработки)

⚠️ **Переменные:**
- base_url должен быть установлен: http://localhost:8000
- Токены сохраняются автоматически при успешной авторизации/регистрации
- ID ресурсов сохраняются автоматически при создании

⚠️ **Даты:**
- Используй формат ISO 8601: 2024-01-15T08:00:00

⚠️ **Авторизация:**
- Все защищенные эндпоинты требуют заголовок: Authorization: Bearer {{token}}
- Используй client_token для запросов клиента
- Используй trainer_token для запросов тренера

### 6. Начни тестирование:

Начни с регистрации клиента и тренера, затем последовательно протестируй все эндпоинты согласно описанным флоу. После каждого запроса проверь ответ и создай тестовый скрипт для автоматической валидации.
```

---

## 🎯 Краткая версия промпта (если нужно быстро протестировать)

```
Протестируй Postman коллекцию "Coach Fit API":

1. Протестируй полный флоу регистрации и авторизации клиента и тренера
2. Проверь все CRUD операции для каждого ресурса (workouts, programs, metrics, nutrition, exercises, notes, finances)
3. Создай тестовые скрипты для каждого запроса (проверка статус кода, структуры ответа, сохранение переменных)
4. Проверь обработку ошибок (400, 401, 403, 404)
5. Убедись что токены и ID автоматически сохраняются в переменные
6. Создай отчет о тестировании с описанием найденных проблем и рекомендаций

Важно: Для регистрации используй только /register/step1 → /register/step2 (НЕ используй /send-sms перед регистрацией)
```

---

## 📝 Как использовать

1. Откройте Postman
2. Импортируйте коллекцию `Coach_Fit_API.postman_collection.json` (если еще не импортирована)
3. Откройте AI Postman (обычно кнопка "AI" или "Ask AI" в интерфейсе)
4. Скопируйте промпт выше и вставьте в поле для AI
5. Нажмите отправить/выполнить
6. AI Postman начнет автоматическое тестирование всех эндпоинтов

## 💡 Советы

- **Длинный промпт** - используйте полную версию для детального тестирования
- **Краткий промпт** - используйте краткую версию для быстрой проверки
- Убедитесь, что сервер запущен на `http://localhost:8000` перед началом тестирования
- Проверяйте консоль сервера для получения SMS кодов в режиме разработки

